interface:
  display_name: "Resonance"
  short_description: "Coordinate via control baton"
  default_prompt: "Use $resonance to bootstrap this change, create control.md, return the /resonance executor <uuid> command, and coordinate with the long-lived Claude Code Executor through the control baton."
